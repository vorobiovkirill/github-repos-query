export type RepositoryData = {
    id: string;
    name: string;
    url: string;
    description: string;
    stars: number;
    language: string;
    avatar: string;
}

export type Language = {
    id: string;
    language: string;
}